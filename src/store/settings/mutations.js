export const SET_ROUTE = (state, route) => {
  state.route = route
}

export const setNonRestrictedMenu = (state, payload) => {
  state.nonRestrictedMenu = payload
}

export const setConfigurationMenu = (state, payload) => {
  state.configurationMenu = payload
}
